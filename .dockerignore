# Dependencies and builds
node_modules
dist
npm-debug.log
yarn-debug.log
yarn-error.log
pnpm-debug.log

# Git files
.git
.gitignore

# OS files
.DS_Store
Thumbs.db

# Jenkins & CI/CD
Jenkinsfile
sonar-project.properties
.scannerwork

# TLS credentials — never bake into the image; mount at runtime
key.pem
cert.pem
*.local

# Documentation and tests
README.md
test.js
image.png

# Examples — NOT part of the proxy Docker image
examples/

# Kubernetes manifests — not needed inside the image
k8s/

# NOTE: config.yaml is NOT excluded here.
# The Dockerfile bakes a default config into the image.
# At runtime, docker-compose.yml mounts your real config.yaml over it.
# Always run: cp config.example.yaml config.yaml  before building.
