apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ninja-proxy
  namespace: ninja-proxy
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ninja-proxy
  minReplicas: 2
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
