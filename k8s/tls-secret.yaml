apiVersion: v1
kind: Secret
metadata:
  name: ninja-proxy-tls
  namespace: default
  labels:
    app: ninja-reverse-proxy
type: Opaque
data:
  # Replace with: cat cert.pem | base64 -w 0
  cert.pem: <BASE64_ENCODED_CERT>
  # Replace with: cat key.pem | base64 -w 0
  key.pem: <BASE64_ENCODED_KEY>
